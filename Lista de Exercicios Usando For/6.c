// 6. Imprimir os quadrados dos números de 1 a 10
#include <stdio.h>

int main() {
    int i;

    // Loop para calcular e imprimir o quadrado de cada número
    for(i = 1; i <= 10; i++) {
        printf("O quadrado de %d é %d\n", i, i * i);
    }

    return 0;
}
